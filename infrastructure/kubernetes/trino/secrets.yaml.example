# Trino S3 Credentials for Garage - EXAMPLE FILE
#
# This is an example file showing the structure of Trino S3 credentials.
# These credentials allow Trino to access Iceberg tables stored in Garage S3.
#
# Copy this file to 'secrets.yaml' before deployment.
#
# IMPORTANT: Do not commit secrets.yaml with actual credentials!
#
# Usage:
#   1. Copy this file: cp secrets.yaml.example secrets.yaml
#   2. Run 'make init' - this creates the Garage access keys
#   3. Get credentials: kubectl exec -n lakehouse garage-0 -- /garage key info lakehouse-access
#   4. Update the values below with the actual Garage credentials
#   5. Apply: kubectl apply -f infrastructure/kubernetes/trino/secrets.yaml

apiVersion: v1
kind: Secret
metadata:
  name: trino-s3-credentials
  namespace: lakehouse
type: Opaque
stringData:
  # Garage S3 Access Key ID (from: kubectl exec -n lakehouse garage-0 -- /garage key info lakehouse-access)
  aws-access-key-id: "GARAGE_ACCESS_KEY_ID_HERE"

  # Garage S3 Secret Access Key (from: kubectl exec -n lakehouse garage-0 -- /garage key info lakehouse-access)
  aws-secret-access-key: "GARAGE_SECRET_ACCESS_KEY_HERE"
