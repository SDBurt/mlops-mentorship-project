# Trino S3 Credentials for MinIO - EXAMPLE FILE
#
# This is an example file showing the structure of Trino S3 credentials.
# These credentials allow Trino to access Iceberg tables stored in MinIO S3.
#
# Copy this file to 'secrets.yaml' before deployment.
#
# IMPORTANT: Do not commit secrets.yaml with actual credentials!
#
# Usage:
#   1. Copy this file: cp secrets.yaml.example secrets.yaml
#   2. Run 'make init' - this creates the MinIO access keys
#   3. Get credentials from MinIO deployment configuration
#   4. Update the values below with the actual MinIO credentials
#   5. Apply: kubectl apply -f infrastructure/kubernetes/trino/secrets.yaml

apiVersion: v1
kind: Secret
metadata:
  name: trino-s3-credentials
  namespace: lakehouse
type: Opaque
stringData:
  # MinIO S3 Access Key ID
  aws-access-key-id: "MINIO_ACCESS_KEY_ID_HERE"

  # MinIO S3 Secret Access Key
  aws-secret-access-key: "MINIO_SECRET_ACCESS_KEY_HERE"
