# Airbyte S3 Storage Credentials Secret - EXAMPLE FILE
#
# This is an example file showing the structure of the Airbyte secrets.
# Copy this file to 'secrets.yaml' before deployment.
#
# IMPORTANT: Do not commit secrets.yaml with actual credentials!
#
# Usage:
#   1. Copy this file: cp secrets.yaml.example secrets.yaml
#   2. Run 'make deploy' and 'make init' - credentials will be auto-populated from Garage
#   3. The Makefile will extract Garage credentials and update secrets.yaml automatically
#
# The actual secrets.yaml is gitignored and will be created/updated during deployment.

apiVersion: v1
kind: Secret
metadata:
  name: airbyte-airbyte-secrets
  namespace: lakehouse
  labels:
    app.kubernetes.io/name: airbyte
    app.kubernetes.io/instance: airbyte
type: Opaque
stringData:
  # Garage S3 credentials (populated automatically during 'make init')
  # These placeholders will be replaced with actual credentials from Garage
  AWS_ACCESS_KEY_ID: "PLACEHOLDER_REPLACE_DURING_INIT"
  AWS_SECRET_ACCESS_KEY: "PLACEHOLDER_REPLACE_DURING_INIT"
