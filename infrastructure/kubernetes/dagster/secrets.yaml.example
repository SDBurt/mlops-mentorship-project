# Dagster PostgreSQL Secret - EXAMPLE FILE
#
# This is an example file showing the structure of the Dagster database secret.
# Copy this file to 'secrets.yaml' and update with your password before deployment.
#
# IMPORTANT: Do not commit secrets.yaml with actual credentials!
#
# Usage:
#   1. Copy this file: cp secrets.yaml.example secrets.yaml
#   2. Generate a strong password: openssl rand -base64 32
#   3. Encode it: echo -n "your-password" | base64
#   4. Update the postgresql-password value below
#   5. Apply: kubectl apply -f infrastructure/kubernetes/dagster/secrets.yaml

apiVersion: v1
kind: Secret
metadata:
  name: dagster-postgresql-secret
  namespace: lakehouse
type: Opaque
data:
  # Base64 encoded password
  # To generate: openssl rand -base64 32 | base64
  # To decode: echo "base64-string" | base64 -d
  postgresql-password: <BASE64_ENCODED_PASSWORD>
