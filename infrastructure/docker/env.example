# Reddit API Credentials
# Get these from https://www.reddit.com/prefs/apps
# Copy this file to .env and fill in your actual credentials

REDDIT_CLIENT_ID=your_client_id_here
REDDIT_CLIENT_SECRET=your_client_secret_here
REDDIT_USER_AGENT=lakehouse:v1.0.0 (by /u/your_username)

# AWS/MinIO Credentials
# Used by Flink, Polaris, MinIO, and Dagster services
# For local development with MinIO, these are typically:
# - Access Key ID: admin (or your MinIO root user)
# - Secret Access Key: password (or your MinIO root password)
# - Region: dummy-region (for local MinIO, not used)

AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_REGION=your_aws_region_here

# MinIO Root Credentials
# These are used by MinIO service itself
# Change these from defaults for production use

MINIO_ROOT_USER=your_minio_root_user_here
MINIO_ROOT_PASSWORD=your_minio_root_password_here

# Polaris Credentials
# Used for Polaris REST catalog authentication
# Format: realm,username,password

POLARIS_REALM=default-realm
POLARIS_USER=root
POLARIS_PASSWORD=secret

# PostgreSQL Credentials (Dagster)
# Used by Dagster services for database connection
# Change these from defaults for production use

POSTGRES_USER=your_postgres_user_here
POSTGRES_PASSWORD=your_postgres_password_here
POSTGRES_DB=your_postgres_database_here

# Dagster PostgreSQL Credentials (aliases)
# These match POSTGRES_* variables above for Dagster services

DAGSTER_POSTGRES_USER=your_dagster_postgres_user_here
DAGSTER_POSTGRES_PASSWORD=your_dagster_postgres_password_here
DAGSTER_POSTGRES_DB=your_dagster_postgres_database_here

# Flink Job Configuration
# Used by Flink SQL jobs for Polaris catalog and Kafka connectivity
# These are parameterized in SQL files (submit-charges-only.sql, etc.)
# Defaults work for Docker Compose local environment

POLARIS_URI=http://polaris:8181/api/catalog
POLARIS_OAUTH_URI=http://polaris:8181/api/catalog/v1/oauth/tokens
KAFKA_BOOTSTRAP_SERVERS=kafka-broker:29092

# Trino Configuration
# Used by Dagster TrinoResource for data quality monitoring
# In Docker Compose, use service name "trino"
# For local development with port-forwarding, use "localhost"

TRINO_HOST=trino
