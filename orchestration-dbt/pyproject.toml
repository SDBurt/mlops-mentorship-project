[project]
name = "transformations"
version = "0.1.0"
description = "DBT transformations for Reddit data lakehouse"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dbt-core>=1.8.0",
    "dbt-trino>=1.8.0",  # Trino adapter for DBT
]
