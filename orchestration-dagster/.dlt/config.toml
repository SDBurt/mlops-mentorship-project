# DLT configuration for filesystem destination with Iceberg tables via Polaris REST catalog
# Credentials are in secrets.toml
# PyIceberg (used by DLT) is configured via environment variables to use Polaris REST catalog

[destination.filesystem]
# Layout for organizing files/tables in S3
layout = "{table_name}/{load_id}.{file_id}.{ext}"

# Non-secret configuration only
# All credentials (aws_access_key_id, aws_secret_access_key, endpoint_url, etc.)
# must be in secrets.toml
